import * as React from "react";
declare const SynchronizeHead: ({ active, children, rtl, width, }: {
    active: boolean;
    children: React.ReactNode;
    rtl: boolean;
    width: number;
}) => React.ReactNode;
export default SynchronizeHead;
